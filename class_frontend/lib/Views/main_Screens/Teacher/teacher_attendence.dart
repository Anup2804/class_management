import 'package:flutter/material.dart';

class TakeAttendence extends StatelessWidget {
  const TakeAttendence({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('take Attendence'),),
    );
    
  }
}